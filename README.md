# React Starterkit
Get a quick start on a new React project.
Includes:
- Material UI
- Redux state

## Getting Started
1. Make sure you have installed `yarn`
2. Install packages with running the command `yarn`
3. Run the application by running the comman `yarn start`