# React Starterkit
Get a quick start on a new React project.
Includes:
- Axios
- Material UI
- Material Table
- React Hook Form
- React Router Dom
- Styled Components

## Getting Started
1. Make sure you have installed `npm`
2. Install packages with running the command `npm install`
3. Run the application by running the comman `npm start`